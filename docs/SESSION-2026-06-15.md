# Operix — Session Handoff (2026-06-15)

Detailed change log + resume context for the session that built the **Knowledge
Base**, **Client Portal (Phase 1)**, and a large batch of **UX / notifications**
work. Read this + `docs/STATUS.md` to pick up cold in a new session.

---

## 0. Quick start (resume checklist)
- **Dev server:** `npm run dev` (port 3000). Stop it before any `npm run db:push`, then restart.
- **DB:** Supabase **Mumbai** (`aws-1-ap-south-1`), **session pooler :5432**. `.env` `DATABASE_URL`/`DIRECT_URL` already set. **`db:push` only — never `db:seed`** (shared real data).
- **Admin login:** `admin@operix.test` / `ChangeMe123!` (Super Admin; has **no** linked Employee, so employee-only flows like Leave-apply don't render for it).
- **Employees with logins:** Keya Panja (`keya@gowithepic.com`, EMP001, Tech) and Suman Roy (EMP002). Keya is currently clocked-in.
- **Verify** via the preview MCP `eval`/`logs` (screenshots time out; viewport resets narrow on restart → `preview_resize` ~1680).
- **AGENTS.md rule:** this is a modified Next.js 16 — read `node_modules/next/dist/docs/` before using framework APIs (e.g. `proxy.ts` is the renamed middleware; runs on Node runtime).

---

## 1. What this session delivered

### Knowledge Base (Module 11) — DONE & verified
- Schema: `KbArticle` (+ `projectId`, `departmentId`, `serviceId`, `updatedById`, markdown `body`), `KbArticleVersion` (change log); relations on Project/Department/Service.
- `lib/kb/markdown.ts` — escape-first XSS-safe Markdown→HTML (isomorphic).
- `lib/kb/actions.ts` — create/update/delete; each save writes a version (editor + time). Anyone can edit; delete = author or `kb:manage`.
- `components/kb/rich-text-editor.tsx` — **hand-rolled WYSIWYG**: contentEditable seeded from `renderMarkdown`, serialized back to Markdown on change (so storage stays Markdown and the safe renderer keeps rendering; no HTML sanitizer needed). Toolbar: B/I/code/H1–3/lists/quote/link.
- `components/kb/kb-form.tsx` — Project → Service (filtered by the project's `ProjectService`) → Department auto-fill; keywords; the editor.
- Pages: `app/(app)/knowledge-base/{page,new,[id],[id]/edit}`.
- **Task ↔ KB link:** `app/(app)/tasks/[id]/page.tsx` shows a **"Related guides"** card for the task's `projectId`+`serviceId` (project-specific first, then general; tagged "This project"/"General").
- Nav: Knowledge Base added (visible to all).

### Client Portal (Module 10) — Phase 1 built (client-side walkthrough pending)
Scope confirmed with the user: **link-based deliverables**, **progress-only** visibility, no file uploads yet.
- **Isolation spine:** `app/portal/` (a real `/portal` segment, NOT a route group — a `(portal)` group collided with `(app)` and threw "two parallel pages"). `proxy.ts` now decodes the JWT role: CLIENT confined to `/portal/*`, staff blocked from it, post-login redirect by role. `lib/auth/guard.ts` → `requirePortal()` / `requirePortalAction()`. `lib/portal/data.ts` centralizes every `clientId`-scoped read.
- **Invites:** `lib/clients/actions.ts` `inviteClient()` (mirrors employee invite) + `components/clients/portal-access.tsx` on the client detail page.
- **Portal pages:** `app/portal/{page (overview), projects, projects/[id], deliverables}` + `components/portal/*` (project card, progress bar, deliverable card, review controls).
- **Approvals:** `lib/portal/actions.ts` — client approves / requests-changes on **CLIENT_REVIEW** tasks and **deliverables**; re-checks ownership; notifies the internal team.
- **Internal deliverables:** `lib/deliverables/actions.ts` + `components/projects/deliverables-panel.tsx` (on the internal project page, when the project has a client).
- **Verified:** internal side (invite card, deliverables publish, staff→/dashboard redirect). **NOT verified:** the CLIENT experience (login lands on /portal, can't reach app, approvals, 404 on another client's URL) — **the auto-mode classifier blocked creating a CLIENT account + sending the invite email**, so a real client login needs the user to set one up.

### Organization / company settings
- New **Company** tab (`components/org/company-info-form.tsx`, `lib/org/actions.ts` `updateCompanyInfo`): name, tagline, **logo URL**, business type, website, email, phone, address. Name + tagline + logo render in the **sidebar footer** (`components/shell/sidebar.tsx`) — replaced the old "Phase 1 · MVP" box.
- Shifts / Locations / Probation **moved into the Company tab**; each section is a **single card** (form + table). Locations toggle redesigned (proper contained switch) and the **add-location form hides when multi-location is off + a location exists**.
- **Day-before reminder setting** (toggle + time) → `lib/calendar/actions.ts` `setEventReminder`.

### User profiles
- Schema: `User` += `nickname`, `avatarUrl`, `bio`.
- `lib/profile/{actions,status}.ts`, `components/ui/avatar.tsx`, `components/profile/profile-edit-form.tsx`.
- `app/(app)/profile` (self edit) + `app/(app)/people/[id]` (public view + **Active/Away** badge from today's attendance). Topbar shows the avatar + "My profile".

### Notifications additions
- `lib/notifications/categories.ts`: new **Announcements** category (sky), `ANNOUNCE*`/`HOLIDAY*` → Announcements, deep-link `/calendar`; "Clients" category now deep-links to `/projects/{projectId}`.
- **Leave** (`lib/leave/actions.ts`): apply → approvers (`leave:approve` roles), approve/reject → employee.
- **Announcements** (`lib/calendar/actions.ts`): posting fans out to everyone (`notifyAllInternal`).
- **Day-before reminders** (`lib/calendar/reminders.ts` `sendEventReminders`): lazy trigger called from `app/(app)/layout.tsx`; idempotent via `reminderSentAt` atomic claim; honest limitation = fires on first page load past the configured time (no cron).

### Smaller UX fixes
- Main content width 1280→**1600px** (`app/(app)/layout.tsx`).
- Sidebar nav reorder (`lib/nav.ts`): Notifications/Calendar/Leave moved to just before Organization.
- **Tasks list** (`components/tasks/tasks-table.tsx`): per-row **Edit/Delete** actions; `deleteTask` (`lib/projects/actions.ts`) hardened to delete all child rows (timers/time-entries/checklist/attachments/deps/assignees/comments + un-parent subtasks).
- **Kanban column order** (`lib/status.ts` `TASK_COLUMNS`): To Do → In Progress → Review → Client Review → Completed → Redo.
- **New project form** (`components/projects/project-form.tsx`): **service search** (hidden checkboxes preserve selection while filtered).
- **Leave apply form** (`components/leave/apply-form.tsx`): disable Submit + warn when the chosen category is exhausted / over-balance.

---

## 2. Schema changes this session (all pushed via `db:push`)
- `KbArticle` + `KbArticleVersion` (+ project/dept/service/updatedBy) — KB.
- `Deliverable` += `description, link, submittedById, decidedById` — link-based deliverables.
- `User` += `nickname, avatarUrl, bio` — profiles.
- `Company` += `tagline, website, email, phone, logoUrl, eventReminderEnabled, eventReminderTime`.
- `Holiday` / `Announcement` += `reminderSentAt`.

---

## 3. Test artifacts left in the DB (clean up if unwanted)
- **KB articles:** "How to prepare the Weekly Audit Report" (general) + "401k Manuever — Audit Report SOP" (project-specific). Both real, useful guides.
- **Deliverable:** "Q2 Audit Report — draft for review" on project **401k Manuever** (status Awaiting client).
- **Announcement:** "Quarterly all-hands meeting" (Jun 20) + the notifications it sent to admin/Keya/Suman.
- **Admin profile:** nickname "Admin" + a placeholder pravatar avatar.
- **Company:** tagline "Your operations, unified" + a placeholder `placehold.co` logo. Event reminder left **off**.
> All editable/deletable from their respective screens.

---

## 4. Known limitations / pending verification
- **Client Portal client-side** — needs a real CLIENT login (user must invite a client + set the password, or authorize a test account). Then verify: login → /portal, can't reach `(app)`, can't see another client's project/deliverable by URL (→404), approve/request-changes, no internal fields leak.
- **Leave notifications + balance-block** — code-complete + `tsc` clean but not exercised live (apply/approve needs an Employee account; admin has none). Fold into the employee-login walkthrough.
- **Day-before reminders + late-check** — lazy (page-load) triggers, not exact-time; a real cron is a deployment add.
- **Images** (avatar/logo/KB) are **URL-based**; real uploads = Phase-2 Supabase Storage.

---

## 5. Standing context / decisions
- **Form Builder** is the planned next phase (drag-drop forms for clients/employees: referral/feedback/onboarding service-selection/POSH; JSON storage; form-data reports). Keep portal onboarding routing modular and reuse `clientId`/`employeeId` scoping so it slots in. Don't build it yet.
- Perf root-cause earlier this build: DB latency. Session pooler + Mumbai region fixed it (~1850ms → ~150ms dashboard).
- Reusable patterns to follow: notification fan-out via `prisma.notification.createMany({ type, title, body, meta })` + the `categorize`/`noteHref` taxonomy; settings toggles via the `Toggle` component in `org-tabs.tsx`; URL-image avatars via `components/ui/avatar.tsx`.
