@AGENTS.md

## Project progress (read these first on a fresh session)
- **`docs/STATUS.md`** — current build state: what's live, conventions, what's pending.
- **`docs/SESSION-2026-06-15.md`** — latest session's detailed change log + resume checklist (DB/login/dev-server notes, schema changes, test artifacts, known limitations).
- **`docs/roadmap.md`** — the original 13-module plan. `docs/architecture.md` — design rationale.

Update `docs/STATUS.md` (and add/update a `docs/SESSION-<date>.md`) at the end of a working session so progress carries across sessions.
